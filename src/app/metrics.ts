/**
 * Minimal metrics placeholder.
 * For the contest we keep it simple and focus on correctness and clarity.
 */
export const metrics = {
  inc: (_name: string) => void 0,
  timing: (_name: string, _ms: number) => void 0,
};
